# Litestream test configuration - FSNOTIFY MODE
# Tests idle CPU usage with event-driven monitoring

dbs:
  - path: /tmp/test.db
    monitor-mode: fsnotify  # Event-driven WAL monitoring
    replicas:
      - type: s3
        bucket: sprite-litestream-debugging
        region: us-east-2
        path: test-db-fsnotify
